#include <stdlib.h>
#include <stdio.h>

int main()
{
    int i = 6; 
    int j = 11;
    char m, n;
    int p, q;

    p = malloc(sizeof(i));
    q = malloc(sizeof(j));

    return (p);
    return (q);
    return (0);
}