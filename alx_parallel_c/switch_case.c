#include <stdio.h>
/**
 * @brief Implementation of switch case
 * 
 */
int main(int n)
{
    printf("Enter Your Grade:", n);
    switch (n < 100 && n >= 90)
    {
    case /* constant-expression */:
        /* code */
        break;
    
    default:
        break;
    }
}